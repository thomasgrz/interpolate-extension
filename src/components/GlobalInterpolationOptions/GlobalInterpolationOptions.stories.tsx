import preview from "#.storybook/preview";
import { GlobalInterpolationOptions } from "./GlobalInterpolationOptions";

const meta = preview.meta({ component: GlobalInterpolationOptions });

export default meta;

export const Default = meta.story();
